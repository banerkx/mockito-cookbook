#!/bin/bash

# shellcheck disable=all

if [[ -t 1 ]]
then
  clear
fi

./upom
sed -i 's@<!-- <versionqq/> -->@<versionqq/>@' pom.xml
sed -i 's@<!-- <propertiesqq/> -->@<propertiesqq/>@' pom.xml

ps1-wrapper ${HOME}/scripts/power-shell/utilities-xml.ps1 -XmlFile ${PWD}/pom.xml -FileType "Ant build"
ps1-wrapper ${HOME}/scripts/power-shell/utilities-xml.ps1 -XmlFile ${PWD}/pom.xml -Schema ${PWD}/maven-4.0.0.xsd -FileType "Maven POM"
stat=${?}
echo "LINE = [${LINENO}] stat = [${stat}]"
echo '--------------------------------------------------------------------------------'
echo ''

